import "./Header.style.css";

const Header = () => {
    return (
        <header className="containerHeader">
            <h1 className='titleHeader'>Ma liste de taches</h1>
        </header>
    );
};

export default Header;